# TODO
- [ ] Confirm and fix login UI overflow error on typing (likely caused by non-scrollable Column + keyboard in LoginScreen).
- [ ] Implement robust login flow by roles stored in Firestore `users` collection.
- [ ] Fix login failure root cause(s): role resolution / Firestore security / missing role doc / alias email.
- [ ] Add clear error messages & safe guards for mounted state.
- [ ] Run `flutter analyze` and `flutter test` (or `flutter run` smoke) after edits.


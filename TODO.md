## TODO - Customers list (admin) + Challan save

- [ ] Add/confirm admin screen UI to list customers from Firebase collection `customers`.
- [ ] Add a customer model/mapping (if needed) and a Firestore stream method if not present.
- [ ] Update Staff/Challan form submission to also save challan data into `customers` collection (as requested).
- [ ] Wire new admin UI section/button to view customers.
- [ ] Run `flutter analyze` / `flutter test` (if available) to ensure build passes.

